# PLAN.md — repo-hand-off

## Context

VISION.md で定義された「開発の状態を保存・共有し、人やAI間でシームレスに引き継ぐCLIツール」の実装計画。
Go + cobra で CLI を構築。MVP は export（HANDOFF.md 生成）と prompt（AI向けコンテキスト出力）に集中する。
非Git環境でも壊れず、取得できる情報だけで動作する。

---

## Phase 1: プロジェクト基盤（Go CLI セットアップ）（完了）

- [x] Go モジュール初期化
- [x] cobra 導入、エントリポイント作成
- [x] ディレクトリ構成確立

```
repo-hand-off/
├── main.go
├── cmd/
│   ├── root.go
│   ├── export.go
│   └── prompt.go
├── internal/
│   ├── collector/
│   │   ├── git.go
│   │   ├── files.go
│   │   └── collector.go
│   └── renderer/
│       ├── handoff.go
│       └── prompt.go
├── go.mod
└── go.sum
```

---

## Phase 2: `handoff export` コマンド（完了）

`handoff export` で HANDOFF.md を生成する。非Git環境では Git セクションをスキップする。

- [x] Git 状態収集（ブランチ、最新コミット、未コミット変更、リモートURL）— Git なしなら graceful skip
- [x] プロジェクトファイル検出・読み取り（VISION.md, PLAN.md, LESSONS.md, README.md, CLAUDE.md）
- [x] 簡易ディレクトリ構造（浅い階層、.gitignore 準拠除外）
- [x] HANDOFF.md 生成・書き出し
- [x] `--output` フラグ（出力先指定、デフォルト: `./HANDOFF.md`）

### HANDOFF.md フォーマット

```markdown
# HANDOFF.md
> Generated by repo-hand-off at {timestamp}

## Project
- Repository: {remote_url or "N/A"}
- Branch: {branch} @ {short_hash}
- Uncommitted changes: {yes/no}

## Vision
{VISION.md 内容 or "Not found"}

## Plan
{PLAN.md 内容 or "Not found"}

## Lessons
{LESSONS.md 内容 or "Not found"}

## Current State
{recent commits summary}

## Directory Structure
{tree output}
```

---

## Phase 3: `handoff prompt` コマンド（完了）

`handoff prompt` で AI 向けコンテキストプロンプトを stdout 出力する。

- [x] export 相当の情報収集 → AI向けプロンプトとして整形
- [x] 指示テンプレート付与（「以下のプロジェクト状態を把握して開発を継続してください」等）
- [x] stdout 出力（markdown 形式）
  > `--format` フラグも実装済み（`markdown` / `xml`）。MVP後予定だった XML 対応を前倒し実装。

---

## Phase 4: テスト & ドキュメント（完了）

- [x] collector パッケージのユニットテスト
- [x] renderer パッケージのユニットテスト
- [x] README.md 作成

---

## Phase 5: 非Gitディレクトリ対応（完了）

設計意図（Phase 2 記載）では非Git環境での graceful skip を想定していたが、実装では `CollectGit` 失敗時に即エラー終了する。修正して非Gitでも動作させる。

### ゴール

- Git情報は "Not available" として扱い、エラーにしない
- ファイル収集（VISION.md, PLAN.md 等）・ディレクトリツリーは通常どおり動作する
- `handoff export` / `handoff prompt` が非Gitディレクトリで正常に動く

### タスク

- [x] `collector.go`: `CollectGit` 失敗時にエラーを返さず、空の `GitInfo` で続行する
  > `Branch != ""` で `RecentCommits` 呼び出しもガード（非Git時の不要な git log 実行を回避）
- [x] `renderer/handoff.go`: Git情報が空の場合 "Not available" を表示する
- [x] `renderer/prompt.go`: XML出力で `<project>` セクション自体を省略
- [x] テスト: 非Gitディレクトリでの `Collect` / レンダリングのテスト追加
  > `TestCollectGitNonGitDir`, `TestRenderHandoffNoGit`, `TestRenderPromptXMLNoGit` の3件追加
- [x] 検証: 非Gitディレクトリで `handoff export` / `handoff prompt` を実行して動作確認

---

## MVP後（将来フェーズ）

- `.handoff.yaml` 設定ファイル
- ~~`--format xml`（Claude向け）~~ → Phase 3 で実装済み
- `handoff import` / `handoff diff`
- MCP サーバー対応
- Homebrew / バイナリ配布

---

## Verification

1. Git リポジトリで `handoff export` → HANDOFF.md 正常生成
2. 非Git ディレクトリで `handoff export` → Git セクションスキップで正常動作
3. `handoff prompt` → stdout に AI 向けプロンプト出力
4. `go test ./...` 全テストパス
